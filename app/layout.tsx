import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Know Your Competitor - AI-Powered Competitive Analysis',
  description: 'Instantly analyze any website\'s strategy, pricing, and performance. Get actionable insights to outsmart your competition in minutes, not months.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
